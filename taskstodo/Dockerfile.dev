# Use a Go base image
FROM golang:1.22-alpine
WORKDIR /taskstodo 

# Copy Go modules files and download dependencies
COPY go.mod go.sum ./
RUN go mod download

# Copy all files
COPY . .
EXPOSE 8080
CMD ["go", "run", "./cmd"]